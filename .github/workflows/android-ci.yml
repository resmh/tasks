name: Android CI

on:
  push:
    branches: [ main-attachments ]
  pull_request:
    branches: [ main-attachments ]
    
  workflow_dispatch:

jobs:
  build:

    runs-on: self-hosted

    env:
      ImageOS: ubuntu18
    
    steps:
    - uses: actions/checkout@v3

    - name: Set up ruby
      uses: ruby/setup-ruby@v1
      with:
        bundler-cache: true
    
    - name: set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle
    
    - name: Grant execute permission for gradlew
      run: |
        - chmod +x gradlew
        - chmod +x update_dependency_diff

    - name: Prepare build
      run: |
        - ./update_dependency_diff

    - name: Build with Gradle
      run: ./gradlew build

